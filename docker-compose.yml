version: "3"

services:
  redis:
    container_name: "redis"
    image: redislabs/redistimeseries
    ports:
      - 6379:6379
  timeseries-metrics:
    container_name: "timeseries-metrics"
    build: .
    # restart: on-failure
    network_mode: "host"
    # ports:
    #   - "127.0.0.1:8080:8080"
    #   - "127.0.0.1:50051:50051"
    volumes:
      - ./config/:/redis-timeseries/config/
    tty: true 